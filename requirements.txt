# Jeeves - Production Dependencies
# Python 3.8+ required

# Core dependencies
requests>=2.28.0

# Optional: WebSocket server for fast responses
# Install with: pip install websockets aiohttp
websockets>=10.0
aiohttp>=3.8.0
aiohttp-sse>=2.0
